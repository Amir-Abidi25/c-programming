/*
Simple programm to read a list of integers with arbitrary lengths
*/
#include <stdio.h>
#include <stdlib.h>

int main() {
    size_t capacity = 5;
    int position = 0;

    // 1. Get heap memory --> void* malloc(size_t size) --> cast into the datatyp you want
    
    // 2. Check for errors
    
    // 4. Do something with your allocated memory
    
    // 3. Free memory and set the pointer to NULL
    
}