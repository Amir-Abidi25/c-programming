/*
Simple C program to copy a file using binary mode.
*/
#include <stdio.h>
#include <stdlib.h>
#include <errno.h>

void copy_file(const char *source, const char *destination) {
    // 1. Create a file pointer for the file

    // 2. Check if the file opened successfully

    // Here we use another file pointer for the destination, so repeat 1 and 2
    
    // 4. Do something with the file(s)

    // 3. Close the files
}

int main () {
    copy_file("binary_files.c", "main_args.c");
    return 0;
}